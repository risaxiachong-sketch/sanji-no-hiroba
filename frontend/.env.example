# Local development can opt into deterministic mock data.
VITE_USE_MOCK=true

# AWS-connected environments must set the deployed API Gateway URL.
# VITE_API_BASE_URL=https://example.execute-api.ap-northeast-1.amazonaws.com/prod